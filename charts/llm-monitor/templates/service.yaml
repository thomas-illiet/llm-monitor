apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-monitor.fullname" . }}
  labels:
    {{- include "llm-monitor.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "llm-monitor.selectorLabels" . | nindent 4 }}
